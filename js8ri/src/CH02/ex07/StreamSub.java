package js8ri.CH02.ex07;

import java.util.stream.Stream;

public class StreamSub {
	public static <T> boolean isFinit(Stream<T> stream) {
		// 無限ストリームに対して、有限かどうかをチェックするメソッドは実装できるの？
		// 判定できるなら、方法がよくわからなかった。
		return true;
	}
}
